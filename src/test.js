
const types = {
    header: 'classes.resume_input_header,',
    text: 'classes.resume_input_header,',
    persDetHeader: 'classes.resume_input_header,',
    persDetPar: 'classes.resume_input_header,'
}

console.log(Object.keys(types))